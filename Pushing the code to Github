# Check Git installation
git --version

# Install Git
yum install git

# Intialize a git repository
Login to your Github repo in GUI and create a repository. Also do create a branch as master by adding a sample test file (master is default branch).
cd /tool/
git init
git status
git add .
git commit -m ‘first commit’
git branch b1
git checkout b1
git branch

# Add your remote git repository
git remote add origin https://github.com/Sherineseelan/repo.git
here, Sherineseelan --> account owner name
      repo.git --> repository name

# Push your image to repository in docker hub
git push -u origin b1
here, Username = Sherineseelan
      Password —> generate a token from developer settings in github —> choose repository specific access and choose your repository

